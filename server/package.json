{
  "name": "focusproof-server",
  "version": "1.1.0",
  "description": "FocusProof backend – AI proxy, certificate verification store (SQLite) & analytics",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
